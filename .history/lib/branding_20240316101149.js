import github from './icons/github.svg'

const github = () => {
  const ionGs = document.querySelectorAll('.ionGh');
  if (ionJs.length > 0) {
    ionJs.forEach((element) => {
      const imageJs = document.createElement('img');
      imageJs.src = svgJs;
      sizing(element, imageJs);
      element.replaceWith(imageJs);
    }
    )